# 眠栖 Somnia · The Constitution

后续所有实现必须服从本文。偏离先改 Constitution，再改代码。Agent 执行细节见 `AGENTS.md`。

---

## Mission

眠栖 Somnia 用可演示的数字化闭环，把睡眠经济从概念变成可操作的酒店服务：用户自主提交睡眠偏好，酒店在三维数字孪生中感知环境并执行场景化调控。平台服务比赛论证，证明「偏好采集 → 场景决策 → 环境执行 → 状态反馈」这条实现路径成立。

### 闭环

```
GuestPreference → SleepSceneEngine → HotelDigitalTwin ⇄ EnvSimulator
                         ↑                    │
                         └──── 环境反馈 ───────┘
```

- **睡眠经济**：把客房从“标准化住宿”变成“可配置的睡眠环境产品”。
- **用户端价值**：用户自己填写睡眠偏好与个人信息，形成可复用的睡眠画像。
- **管理端价值**：酒店用三维模型感知房间环境（温度、湿度、光照、噪音），并调节空调等设备。
- **创新点**：偏好不是停留在问卷，而是映射为可执行的睡眠场景（目标温度、湿度、灯光、白噪音、窗帘），并在数字孪生中被看见、被调控。

演示叙事：用户填偏好并选房/服务 → 系统生成睡眠场景 → 管理员在三维酒店中看到房间状态并一键适配/微调，同时处理实景图与服务需求。

### 约束

- 先闭环，后扩张。任何功能必须服务上述四步之一。
- 管理端主界面必须是三维孪生指挥舱，不能退化成纯表格后台。
- Django SimpleUI 只负责数据管理，不能替代 `/manager/twin`。
- 温湿度等环境数据来自仿真器，不假装已接真实温湿度传感器。
- 实时监控只允许接入**走廊/楼道真实摄像头**；**禁止任何客房内监控**，画面不得使用房间实景图或指向房间内部。
- 中文界面，术语统一：睡眠画像、睡眠场景、数字孪生、环境仿真。
- 比赛可演示优先于工程完备。
- 后端依赖必须装在 `backend/.venv`，禁止用系统全局 Python 直接跑服务。

### 角色与功能边界

统一入口，两种登录方式，登录后进入完全不同的工作台。使用账号密码 + 角色分流，不做微信/短信登录。

**用户端（Guest）**

- 注册 / 登录
- 左侧导航三个入口：用户偏好、房间选择、酒店服务
- 用户偏好：个人信息 + 睡眠偏好问卷；用户确认后上传后端（服务端仍静默生成睡眠画像，供管理端一键应用；住客界面不展示「生成睡眠画像」）
- 房间选择：右侧三维酒店模型点选房间；右上角显示该房实景图，右下角确认选择该房间
- 酒店服务：勾选酒店可提供的配套服务并提交

**酒店管理端（Manager，前台三维）**

- 左侧导航：点「酒店运维」进入三维指挥舱；点展开后才出现「实时温湿 / 实时监控」。另有房间更新、用户需求
- 实时温湿：原指挥舱（中三维幕布、左温度查询/趋势、右调控与总览、底仿真播放）
- 实时监控：主区是走廊实况墙（焦点画面 + 三层缩略）；右侧三维只作走廊范围定位；禁止客房内镜头
- 住客提交偏好后，若已选房则立即绑定该房，管理端轮询即时更新
- 房间更新：为每间客房上传/更换实景图
- 用户需求：下设「未完成服务 / 已完成服务」两个入口，先按房间列出，点开房间后才看住客信息与服务项
- 设备调控后，后端仿真器让环境值逐步靠近目标值

**数据后台（Django SimpleUI）**

- `/admin/` 维护住户偏好、客户选房（已入住/已退房）、睡眠偏好、上传流水、服务需求、客房实景、服务目录、前台账号（客户 / 酒店管理员）、仿真开关
- 给评委和开发看库内数据，不承担三维演示主路径

**首期明确不做**：真实支付、OTA 预订、工单系统、真实 MQTT 硬件、多酒店集团权限、移动原生 App。

### 信息架构

- `/login`：同一页两个入口卡片（用户 / 酒店管理人员）
- `/guest/preference`：用户偏好（确认后上传后端）
- `/guest/rooms`：房间选择（三维模型 + 实景图 + 确认）
- `/guest/services`：酒店服务多选提交
- `/manager/twin`：实时温湿指挥舱（中幕布 + 左温度 + 右调控）
- `/manager/monitor`：走廊实况墙 + 三维范围定位（禁止客房内）
- `/manager/rooms`：房间实景图更新
- `/manager/requests`：用户需求（未完成 / 已完成，按房间点开查看）
- `/admin/`：SimpleUI 数据后台
- 运维页房间详情不做独立整页，落在指挥舱右侧面板

演示数据固化：1 栋 3 层、每层 4 间（共 12 间）。

---

## Tech stack

### 前端（必须遵守）

- Vue 3 + TypeScript + Vite
- Vue Router 4：按角色拆路由，`guest` / `manager` 守卫
- Pinia：登录态、用户画像、房间状态（以 API 为权威源）
- Three.js：酒店楼体、房间、状态着色、点击拾取
- TresJS：用 Vue 组件组织三维场景
- Element Plus：表单、登录、筛选、开关
- ECharts：室温/湿度趋势与柱状图
- Axios：`/api` + Bearer；Vite 代理到 Django `127.0.0.1:8000`

### 后端（必须遵守）

- Python 3.11 + 虚拟环境 `backend/.venv`
- Django + django-simpleui：数据后台 UI
- Django REST Framework：前台所需 REST API
- SQLite（`backend/somnia_django.db`）：零运维
- 规则引擎与环境仿真：`backend/app/engine/`（权威实现）
- 仿真由后端线程推进；前台轮询房间与趋势

已废弃且不得无故恢复：FastAPI、SQLAlchemy、纯 localStorage 后端。

### 三维与仿真约定

- 首期不用 Blender 精模，用 Three.js 程序化几何（楼板、墙、房间盒体）
- 房间材质色随温度映射（偏冷蓝 / 舒适绿 / 偏热橙）；也可按占用着色
- 点房间打开右侧调控；空调设定变化后，仿真器按步长逼近目标温度
- 预留 `gltf` 替换口，但不作为第一阶段依赖
- 温湿度「感知视窗」必须标明环境仿真
- 实时监控只覆盖走廊，三维高亮不得扩到客房内部

### 账号约定（演示用）

- 前台：`guest@somnia.demo` / `manager@somnia.demo`，密码 `somnia123`
- SimpleUI：`admin` / `somnia123`
- 角色字段 `guest | manager`，前端路由与后端都校验

### 默认假设

- 物联网：纯数字孪生仿真，不接真实空调/传感器
- 酒店规模：1 栋 3 层 12 房
- 智能决策：规则引擎，首期不接大模型
- 语言：简体中文 UI
- 项目名：眠栖 Somnia

---

## Roadmap

### R0 宪法冻结 — 已完成

- 确认 Mission / 角色 / 页面 / 技术栈并写入本文件

### R1 工程骨架与双登录 — 已完成

- Vite + Vue 3 + TS
- 登录页双入口、路由守卫、会话

### R2 用户端偏好与睡眠画像 — 已完成

- 个人信息 + 睡眠偏好表单
- 可解释规则引擎：偏好 → 推荐场景与设备设定
- 画像结果可供管理端读取

### R3 管理端三维数字孪生 — 已完成

- TresJS 3 层 12 房、点击拾取、温度/占用着色
- 指挥舱三栏：中幕布、左可视化、右调控

### R4 调控闭环 — 已完成

- 空调、灯光、窗帘、白噪音、加湿可调
- 仿真器响应设定值
- 绑定住客 + 一键应用睡眠场景
- 总览：在住、均温、待适配

### R4.5 持久化与后台 — 已完成

- Django + DRF 接管登录、画像、房间、仿真 API
- django-simpleui 数据管理界面
- 种子数据：`manage.py seed_demo`

### R5 比赛打磨 — 进行中

- 3 分钟演示脚本（用户填写 → 管理端看见并调控）
- 空态/错误态、启动说明（README / AGENTS.md）
- 如需论文附图：导出架构图、闭环图、界面截图
